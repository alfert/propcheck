name: Elixir CI

on: push

env:
  MIX_ENV: test
  ELIXIR_ASSERT_TIMEOUT: 2000

jobs:
  nothing:
    name: Not a real job
    runs-on: ubuntu-latest
    steps:
      - name: Some output
        run: echo "Hello, world"
